---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import { ROOT_ALTERNATES } from "../../../data/root-alternates";
import { GUIDE_ROUTE_ITEMS } from "../../../data/i18n-route-manifest";
import { STANDARD_I18N_LOCALES, type ComLocale } from "../../../config/i18n";
import { withLocale } from "../../../lib/locale-paths";

export function getStaticPaths() {
  return STANDARD_I18N_LOCALES.map((locale) => ({
    params: { locale },
    props: { locale },
  }));
}

const { locale } = Astro.props as { locale: ComLocale };
const pagePath = `/${locale}/guides/`;
---

<BaseLayout
  title={`LocalVRAM (${locale.toUpperCase()}) Guides`}
  description="Localized guides route skeleton aligned to English guide slugs."
  locale={locale}
  canonicalPath={pagePath}
  alternates={ROOT_ALTERNATES}
  noindex={true}
>
  <main class="container">
    <section class="hero">
      <h1>Guides Hub ({locale.toUpperCase()})</h1>
      <p>
        Guide route parity is enabled. During rollout, page copy may temporarily fall back to English.
      </p>
      <div class="cta-row">
        <a class="btn btn-primary" href={withLocale(locale, "/en/")}>Back to Locale Home</a>
        <a class="btn" href="/en/guides/best-coding-models/">Open English Guides</a>
      </div>
    </section>

    <section class="table-card">
      <table>
        <thead>
          <tr>
            <th>Guide</th>
            <th>Localized Path</th>
            <th>English Source</th>
          </tr>
        </thead>
        <tbody>
          {GUIDE_ROUTE_ITEMS.map((item) => (
            <tr>
              <td>{item.title}</td>
              <td><a href={withLocale(locale, item.enPath)}>{withLocale(locale, item.enPath)}</a></td>
              <td><a href={item.enPath}>{item.enPath}</a></td>
            </tr>
          ))}
        </tbody>
      </table>
    </section>
  </main>
</BaseLayout>
