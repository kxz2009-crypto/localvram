# Python runtime dependencies
# This repository's Python automation scripts currently use only the standard library.
# Keep this file to make dependency policy explicit and future additions reviewable.
